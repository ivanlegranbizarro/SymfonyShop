<header class="bg-white shadow-md sticky top-0 z-50">
  <div class="container mx-auto px-4 py-4 flex items-center justify-between">
    <a href="{{ path('app_home') }}" class="text-2xl font-bold text-blue-600">Symfony-Shop</a>
    <button id="menu-toggle" class="md:hidden text-gray-600 focus:outline-none">
      ☰
    </button>
    <div class="hidden md:flex items-center space-x-8">
      <nav class="flex space-x-6 text-gray-700 font-medium">
        <a href="{{ path('app_product') }}" class="hover:text-blue-600 transition">Products</a>
        <a href="{{ path('app_home') }}" class="hover:text-blue-600 transition">Home</a>
      </nav>
      <div class="flex items-center space-x-4">
        {% if app.user %}
        <a href="#" class="relative text-blue-600 hover:text-blue-800">
          Cart 🛒
          <span
            class="absolute -top-2 -right-3 bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center">
            <span>
              0
            </span>
          </span>
        </a>
        <a href="#"
          class="text-sm px-4 py-2 rounded-lg bg-green-500 text-white hover:bg-green-600 transition">Dashboard</a>
        <a href="{{ path('app_logout') }}"
          class="text-sm px-4 py-2 rounded-lg bg-red-500 text-white hover:bg-red-600 transition">Logout</a>
        {% else %}
        <a href="{{ path('app_login') }}"
          class="text-sm px-4 py-2 rounded-lg border border-blue-500 text-blue-500 hover:bg-blue-50 transition">Login</a>
        <a href="{{ path('app_register') }}"
          class="text-sm px-4 py-2 rounded-lg border border-blue-500 text-blue-500 hover:bg-blue-50 transition">Register</a>
        {% endif %}
      </div>
    </div>
  </div>
</header>
